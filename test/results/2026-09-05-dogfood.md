# Dogfood log 2026-09-05

Per CLAUDE.md's Dogfooding protocol: run from a consumer project, install
from `dist/` only. Project: `orchestrator-scratch`. Bundle version:
`2026-09-05-1708054` (source commit `7437562`). This is test/harness/
empirical-checklist.md item E13.

## Run 1

Task given (Jeb did not specify a worker; routing was the orchestrator's
own call): "Create a Python-based AI authorship detector for text."

Cell chosen: `worker-sonnet-medium`.

Agreed: no. Jeb's read: sonnet-medium fits mechanical work such as parsing
configuration files or writing text files, nothing needing more than basic
judgement. This task had no spec and no tests, an open-ended build call,
which by ROUTING.md's own axes reads closer to structured-or-open
sensitivity, not mechanical, so sonnet-medium under-provisioned it.

Performance stats: not captured (lost before recording).

## Run 2

Task given: rebuild the same tool ("Rebuild the AI-authorship-detection
tool at `orchestrator-scratch/ai_authorship_detector.py`, but this time
grounded in real research rather than ad-hoc heuristics. A prior worker
already built a quick stdlib-only heuristic version (stylometric signals:
AI-tic phras[ing]...)" - remainder of the prompt not recorded verbatim).

Cell chosen: `worker-fable-xhigh`.

Agreed: no. Jeb's read: opus-xhigh would have been sufficient; fable-xhigh
is over-provisioned for this task. Fable is this repository's row for
sustained, self-directed investigation that reshapes its own plan
(ROUTING.md); a research-grounded rebuild with a clear brief may not need
that, even though it is open and long.

Performance: 6m 14s, 102.9k tokens, 53 tool calls, Fable 5.1 (xhigh).
Output: a Python source file (`ai_authorship_detector.py`, rebuilt).

## Summary

Two runs, two disagreements, in opposite directions: run 1 under-provisioned
(sonnet-medium where sonnet-high fits Jeb's read), run 2 over-provisioned
(fable-xhigh where opus-xhigh fits Jeb's read). Two data points on two
different starting cells does not meet CLAUDE.md's bar ("three or more
disagreements on one starting cell") for concluding the rubric is wrong for
either task class; both are recorded here as calibration input for when
that bar is reached.

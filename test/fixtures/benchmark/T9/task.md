A performance problem is written up in `PROBLEM.md`. Read it, then make
`build_report()` meet the budget it states. Follow the acceptance criteria
in that file, including writing your timing evidence to `MEASUREMENT.txt`.

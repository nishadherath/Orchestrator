Rename the function `compute_total`, defined in `calc.py`, to `compute_sum`,
and update every call site and import so the code keeps working exactly as
before. Do not change any other behaviour, and do not touch the test files.

When you are done, run this yourself and confirm it passes before reporting
back:

    python3 -m unittest discover -s . -p "test_*.py"

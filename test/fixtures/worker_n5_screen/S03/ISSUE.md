An incident report claims the named `suspect` stage is the bottleneck. Verify
`solve(data)` in `app.py` against total cost, computed as count times
milliseconds per stage. Return the largest-total stage as `bottleneck` and
whether it contradicts the suspect. Ties choose the lexicographically first
stage. Repair only if behaviour is wrong; preserve a correct implementation.
Keep the JSON-line CLI.

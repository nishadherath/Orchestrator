# Preserve the early compatibility rule through continuation

Read `COMPATIBILITY.md`, then add the new framed encoding described in
`adapter/samples.json`. The early legacy-byte requirement remains binding after
handoff. Change only `adapter/codec.py`.

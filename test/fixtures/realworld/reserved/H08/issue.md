# Repair two independent modules and run integration checks

Round monetary totals using decimal half-up semantics and normalise labels by
trimming and collapsing whitespace. The modules do not depend on one another.
Change only `billing/pricing.py` and `labels/normalise.py`.

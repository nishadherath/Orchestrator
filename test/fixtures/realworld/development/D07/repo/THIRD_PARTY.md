# Third-party dependency

`p-limit` 7.3.2 is an MIT-licensed dependency from
<https://github.com/sindresorhus/p-limit>. The fixture contains authored
consumer code and deterministic test doubles, not a copy of the package.
`provenance.lock.json` binds the upstream package metadata used by the fixture.

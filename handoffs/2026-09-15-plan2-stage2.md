# Handoff: plan2-stage2

<!-- handoff.py new --slug plan2-stage2 --reason model-change --to-model sonnet --to-effort high --project . -->
Written 2026-09-15 by fable, high. Reason: model-change.

## Goal

Execute Stage 2 of `docs/PLAN-2.md`: the resolver and the ledger. Extend
`tools/route.py` so it resolves a cell from an assessment line plus a
per-project outcome ledger seeded by `src/routing_priors.json`, decides
when the Controller runs, and can record outcomes; add the ledger schema;
build the replay and backtest harnesses that validate it against recorded
data; wire four new `check.py` checks. Zero live `claude -p` calls.

## Decisions already made

- D64 (`docs/DECISIONS.md`): judge and resolver are separate; the ledger
  is the only learning mechanism; the Controller's proactive rule is
  expected-cost arithmetic plus a policy dial labelled as policy; the
  ladder order is floor, `worker-opus-high`, controller, `worker-opus-max`;
  `self_directed` is recorded but not an input to resolution; the
  reporting bar stays in the harness, never in the priors. None of these
  may change in Stage 2 without a new decision entry.
- Every format, function signature, CLI flag, selftest scenario and pass
  condition is fixed in `docs/ROUTING-2-DESIGN.md` sections 1 to 6.
  Implement to it; where it is silent, choose the boring option and note
  the choice in the commit message.
- Existing `route.py` functions (`resolve`, `matching_rules`,
  `resolve_two_axis`) keep their signatures and behaviour: `check.py`'s
  TABLE-DATA and ROUTE-TOTAL call them.

## Files and links that matter

- `docs/ROUTING-2-DESIGN.md` (the spec), `docs/PLAN-2.md` (Stage 2's task
  list and exit criteria), D64 in `docs/DECISIONS.md`.
- `src/routing_priors.json` and `tools/generate_priors.py` (the priors
  and their generator; ROUTE-PRIORS asserts they match).
- `src/cost_table.json` (unit costs with provenance; the projection source).
- `tools/route.py` (extend), `tools/validate_records.py` and
  `src/System/schemas/` (add `RoutingLedgerEntry.schema.json`),
  `test/fixtures/system/{valid,broken}.jsonl` (add ledger lines).
- `test/harness/score_routing.py` (`TWO_STAGE_RE_3AXES` and the prose
  verdict regex are the two formats the replay must parse),
  `test/results/*routing*` (replay input), `test/results/*benchmark*.md`
  (backtest input), `docs/FRONTIERS.md` (task to bucket mapping).
- `test/harness/check.py` (add ROUTE-PRIORS, COST-TABLE, REPLAY, BACKTEST).

## Verified facts

- Harness green at `a9d4460`, 20 checks.
- `tools/generate_priors.py` regenerates `src/routing_priors.json` byte
  for byte (run 2026-09-15).
- On the shipped priors the expected-cost Controller rule fires in no
  bucket: worst case `open/medium/contained` at about USD 0.64 expected
  ladder cost against USD 2.99 for the Controller (2.623 + 0.363). Only
  the policy dial (open, consequential) makes `first: controller`.
- The benchmark staircase climbed only after a floor failure, so the
  recorded pass rates at higher cells are the conditional the rung priors
  need (D64 point 2).
- The recorded two-stage batches (`test/results/2026-09-11-routing-*-d65b476-two-stage-3axis-*`)
  carry field values in section 1's format; the prose batches carry
  `assessment: a, b, c; worker: w; action: x`.

## Work completed

Stage 1 in full: `docs/PLAN-2.md`, D64, `src/cost_table.json`,
`src/routing_priors.json` with its generator, `docs/ROUTING-2-DESIGN.md`,
this handoff. Commits `c381aa2` through `a9d4460` plus this file's own.

## Unresolved questions

- Whether the replay's pooled agreement on the two-stage opus batches
  clears D40's 92.2 percent under the new resolver (section 4's pass
  condition). If it does not, stop and record why (D64's reversal clause);
  do not tune the priors to make it pass.
- Whether the backtest activates any rung the benchmark refuted (section
  5). Same rule.
- The orchestrator's pass/fail judgement, which the ledger will learn
  from, has no measured reliability. Out of scope for Stage 2; note it in
  the ledger schema's description.

## Exact next action

Confirm the session is on sonnet at high effort, read
`docs/ROUTING-2-DESIGN.md` sections 1 to 6 in full, then start task 2.1:
`src/System/schemas/RoutingLedgerEntry.schema.json` with the fixture lines,
`python3 test/harness/check.py` green, one commit, tick the box.

## Model and effort to set

`/model` sonnet, effort high. No worker cell: this is a session handoff.

## Cost projection

Live API spend: USD 0.00 (no `claude -p` calls in Stage 2; every
validation runs on recorded data). Session load: about 44,741 tokens read
before the first task (`src/cost_table.json` session row, chars/4, an
estimate, 2026-09-15), paid once and cached for the stage. Session cost
for the stage, estimated since no telemetry exists for it: USD 10 to 25
equivalent.

## Time projection

Stage 2 as scoped: 2 to 4 hours of session time (six tasks, two new
harness scripts of a few hundred lines each, the `route.py` extension of
similar size, no waiting on runs). No wall-clock component from
`claude -p`: 0 s.

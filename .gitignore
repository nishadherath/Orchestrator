__pycache__/
*.pyc
/dist-with-rationale/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Paid evaluation output is durable local evidence, never release payload.
/pilot-runs/
